================================================================================
  ✅ POSTGRESQL CREDENTIALS UPDATED - v12.1
================================================================================

The PostgreSQL database credentials have been updated to your specified values:

  Database Name: ibiki_sms
  Username:      admin
  Password:      c0smic4382
  Host:          localhost
  Port:          5432

================================================================================
  📦 UPDATED PACKAGES READY
================================================================================

TWO PACKAGES AVAILABLE:

1. LIGHTWEIGHT (RECOMMENDED - Easy to Download)
   File: ibiki-sms-v12.1-SCRIPTS-ONLY.tar.gz (1.1 MB)
   ✅ Easy to download from Replit
   ✅ Auto-installs node_modules on server
   
2. FULL PACKAGE (If needed)
   File: ibiki-sms-v12.1-FULL-DEPLOYMENT.tar.gz (74 MB)
   ✅ Includes node_modules pre-installed
   ❌ Large file (may be hard to download)

================================================================================
  🔐 DATABASE CONNECTION STRING
================================================================================

Your DATABASE_URL will be automatically configured as:

postgresql://admin:c0smic4382@localhost:5432/ibiki_sms

This is set automatically by the setup-postgres.sh script.

================================================================================
  ⚡ DEPLOYMENT (No Changes!)
================================================================================

Same ONE-COMMAND deployment as before:

1. Download package
   Find: ibiki-sms-v12.1-SCRIPTS-ONLY.tar.gz
   Right-click → Download

2. Upload to server
   scp ibiki-sms-v12.1-SCRIPTS-ONLY.tar.gz root@151.243.109.79:/root/

3. Extract and deploy
   ssh root@151.243.109.79
   cd /root
   tar -xzf ibiki-sms-v12.1-SCRIPTS-ONLY.tar.gz
   cd ibiki-sms
   
   sudo ./full-deploy.sh

4. Open browser
   http://151.243.109.79

================================================================================
  📝 WHAT CHANGED IN v12.1
================================================================================

CHANGED:
✅ PostgreSQL username: ibiki_user → admin
✅ PostgreSQL password: (random) → c0smic4382
✅ .env.example updated with correct DATABASE_URL
✅ setup-postgres.sh uses your credentials

UNCHANGED:
✅ Database name: ibiki_sms (same)
✅ Application code (same)
✅ Deployment process (same)
✅ All features (same)

================================================================================
  🔍 VERIFY AFTER DEPLOYMENT
================================================================================

After running full-deploy.sh, verify credentials:

# Check .env file
cat /root/ibiki-sms/.env | grep DATABASE_URL

# Should show:
DATABASE_URL=postgresql://admin:c0smic4382@localhost:5432/ibiki_sms

# Test database connection
sudo -u postgres psql -d ibiki_sms -c "\du"

# Should show user: admin

# Test login to database
PGPASSWORD=c0smic4382 psql -U admin -d ibiki_sms -h localhost -c "SELECT 1;"

# Should return: 1

================================================================================
  🎯 FOR YOUR SERVER (151.243.109.79)
================================================================================

If you already deployed v12.0 and want to update to v12.1:

OPTION 1: Fresh Install (Recommended)
--------------------------------------
sudo -u postgres dropdb ibiki_sms 2>/dev/null || true
sudo -u postgres dropuser ibiki_user 2>/dev/null || true
sudo -u postgres dropuser admin 2>/dev/null || true
PM2_HOME=/home/ibiki/.pm2 pm2 delete ibiki-sms 2>/dev/null || true

cd /root
tar -xzf ibiki-sms-v12.1-SCRIPTS-ONLY.tar.gz
cd ibiki-sms
sudo ./full-deploy.sh


OPTION 2: Update Existing Database Credentials
-----------------------------------------------
# Change database username and password
sudo -u postgres psql << EOF
ALTER USER ibiki_user RENAME TO admin;
ALTER USER admin WITH PASSWORD 'c0smic4382';
EOF

# Update .env file
nano /root/ibiki-sms/.env
# Change to: DATABASE_URL=postgresql://admin:c0smic4382@localhost:5432/ibiki_sms

# Restart application
PM2_HOME=/home/ibiki/.pm2 pm2 restart ibiki-sms --update-env

================================================================================
  ✅ SUMMARY
================================================================================

Your PostgreSQL credentials are now:
  Username: admin
  Password: c0smic4382

Download the updated package and deploy with:
  sudo ./full-deploy.sh

The script will automatically create the database with these credentials!

================================================================================
