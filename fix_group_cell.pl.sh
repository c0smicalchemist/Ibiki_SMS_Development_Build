perl -0777 -i -pe "s|<TableCell[\s\S]*?placeholder=\"GROUP-ID\"[\s\S]*?</TableCell>|<TableCell className=\"py-2\">\n  <Input defaultValue={client.groupId || ''} placeholder=\"GROUP-ID\" className=\"w-32 h-8 text-xs\" onBlur={(e) => {\n    const v = e.target.value.trim();\n    apiRequest(\/api\/admin\/users\/\\/group, { method: 'POST', body: JSON.stringify({ groupId: v }) })\n      .then(() => queryClient.invalidateQueries({ queryKey: ['\/api\/admin\/clients'] }));\n  }} />\n</TableCell>|g" /opt/ibiki-sms/client/src/pages/AdminDashboard.tsx
